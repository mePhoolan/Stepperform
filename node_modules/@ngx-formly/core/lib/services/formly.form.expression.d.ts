import { FormGroup, FormArray } from '@angular/forms';
import { FormlyFieldConfig, FormlyFormOptions } from '../components/formly.field.config';
/**
 * @internal
 */
export declare class FormlyFormExpression {
    checkFields(form: FormGroup | FormArray, fields: FormlyFieldConfig[], model: any, options: FormlyFormOptions): void;
    private _checkFields(form, fields, model, options);
    private checkFieldExpressionChange(form, field, model, options);
    private checkFieldVisibilityChange(form, field, model, options);
    private addFieldControl(parent, field, model);
    private getFieldModel(model, field);
    private getParentModel(model, field);
    private removeFieldControl(parent, field);
    private fieldParentFormControl(form, field);
    private fieldKey(field);
}
