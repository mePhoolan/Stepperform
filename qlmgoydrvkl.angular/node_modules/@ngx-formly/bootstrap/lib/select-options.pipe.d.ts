import { PipeTransform } from '@angular/core';
import { Observable } from 'rxjs';
import { FormlyFieldConfig } from '@ngx-formly/core';
export declare class FormlySelectOptionsPipe implements PipeTransform {
    transform(options: any, field?: FormlyFieldConfig): Observable<any[]>;
    private toOptions(options, field);
    private toOption(item, to);
    private getLabelProp(item, to);
    private getValueProp(item, to);
    private getDisabledProp(item, to);
    private getGroupProp(item, to);
}
