import { DoCheck, OnChanges, SimpleChanges, EventEmitter, OnDestroy, ComponentFactoryResolver } from '@angular/core';
import { FormGroup, FormArray, NgForm, FormGroupDirective } from '@angular/forms';
import { FormlyFieldConfig, FormlyFormOptions } from './formly.field.config';
import { FormlyFormBuilder } from '../services/formly.form.builder';
import { FormlyConfig } from '../services/formly.config';
export declare class FormlyForm implements DoCheck, OnChanges, OnDestroy {
    private formlyBuilder;
    private formlyConfig;
    private componentFactoryResolver;
    private parentForm;
    private parentFormGroup;
    private parentFormlyForm;
    model: any;
    form: FormGroup | FormArray;
    fields: FormlyFieldConfig[];
    options: FormlyFormOptions;
    modelChange: EventEmitter<any>;
    /** @internal */
    isRoot: boolean;
    private initialModel;
    private modelChangeSubs;
    private enableCheckExprDebounce;
    private checkExpressionChange$;
    constructor(formlyBuilder: FormlyFormBuilder, formlyConfig: FormlyConfig, componentFactoryResolver: ComponentFactoryResolver, parentForm: NgForm, parentFormGroup: FormGroupDirective, parentFormlyForm: FormlyForm);
    ngDoCheck(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    changeModel(event: {
        key: string;
        value: any;
    }): void;
    setOptions(): void;
    private checkExpressionChange;
    private trackModelChanges;
    private clearModelSubscriptions;
}
